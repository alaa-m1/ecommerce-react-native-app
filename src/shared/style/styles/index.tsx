import {ViewStyle} from 'react-native';

export const sharedStyles: Record<string, ViewStyle> = {
  page: {
    height: '100%',
    flexGrow: 1,
    paddingHorizontal: 5,
    paddingVertical: 5,
    backgroundColor: '#fff',
    borderColor: '#fff',
    borderWidth: 10,
    borderRadius: 20,
  },
};
